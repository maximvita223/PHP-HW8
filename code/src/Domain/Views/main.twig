<!DOCTYPE html>

<html random={{ random_int }}>
    <head>
        <title>{{ title }}</title>
        <style>{{ style }}</style>
    </head>
    <body>
        {% include 'header.twig' %}
        <div class="container">
            <div class="box-left">
                {% include 'sidebar.twig' %}
            </div>
            <div class="box-right">
                {% if islogin %}
                    <p>Добро пожаловать, {{ username }}</p>
                {% endif %}
                {% include content_template_name %}
            </div>
        </div>
        {% include 'footer.twig' %}
    </body>
</html>